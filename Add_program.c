#include <stdio.h>
#include <conio.h>

void main()
{
    int a, b, c;
    printf("Enter the value of a & b : ");
    scanf("%d%d", &a, &b);
    c = a + b;
    printf("Additio is : %d", c);
    getch();
}
